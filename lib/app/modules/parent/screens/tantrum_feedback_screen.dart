import 'package:flutter/material.dart';

class TantrumFeedbackScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tantrum Feedback'),
      ),
      body: Center(
        child: Text('Tantrum Feedback Screen'),
      ),
    );
  }
}
