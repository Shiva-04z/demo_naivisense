import 'package:flutter/material.dart';

class CaseLoadScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Case Load'),
      ),
      body: Center(
        child: Text('Case Load Screen'),
      ),
    );
  }
}
